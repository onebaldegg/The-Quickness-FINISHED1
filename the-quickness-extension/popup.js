/* global chrome */
// THE QUICKNESS - Popup Script (Downloads Folder Only)

document.addEventListener('DOMContentLoaded', () => {
  console.log('THE QUICKNESS popup loaded - Downloads folder only');
  // Simple popup, no folder selection needed
});